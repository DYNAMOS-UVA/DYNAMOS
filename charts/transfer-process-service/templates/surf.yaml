apiVersion: apps/v1
kind: Deployment
metadata:
  name: transfer-process-service-surf
  namespace: {{ .Values.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transfer-process-service-surf
  template:
    metadata:
      labels:
        app: transfer-process-service-surf
      annotations:
        "linkerd.io/inject" : enabled
    spec:
      containers:
      - name: transfer-process-service-surf
        image: {{ .Values.dockerArtifactAccount }}/transfer-process-service:{{ .Values.branchNameTag }}
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: DATA_STEWARD_NAME
          value: SURF
        - name: ETCD_ENDPOINTS
          value: {{ .Values.etcdEndpoints | quote }}

---

apiVersion: v1
kind: Service
metadata:
  name: transfer-process-service-surf
  namespace: {{ .Values.Namespace }}
spec:
  selector:
    app: transfer-process-service-surf
  ports:
    - name: http-transfer-process-service-api
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
