apiVersion: apps/v1
kind: Deployment
metadata:
  name: transfer-process-service-uva
  namespace: {{ .Values.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transfer-process-service-uva
  template:
    metadata:
      labels:
        app: transfer-process-service-uva
      annotations:
        "linkerd.io/inject" : enabled
    spec:
      containers:
      - name: transfer-process-service-uva
        image: {{ .Values.dockerArtifactAccount }}/transfer-process-service:{{ .Values.branchNameTag }}
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        env:
        - name: DATA_STEWARD_NAME
          value: UVA
        - name: ETCD_ENDPOINTS
          value: {{ .Values.etcdEndpoints | quote }}

---

apiVersion: v1
kind: Service
metadata:
  name: transfer-process-service-uva
  namespace: {{ .Values.Namespace }}
spec:
  selector:
    app: transfer-process-service-uva
  ports:
    - name: http-transfer-process-service-api
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
